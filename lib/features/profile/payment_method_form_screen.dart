import 'package:flutter/material.dart';

class PaymentMethodFormScreen extends StatelessWidget {
  const PaymentMethodFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PaymentMethodFormScreen')),
      body: const Center(
        child: Text('PaymentMethodFormScreen content goes here'),
      ),
    );
  }
}

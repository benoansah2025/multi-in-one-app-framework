import 'package:flutter/material.dart';

class LiveOrderStatusScreen extends StatelessWidget {
  const LiveOrderStatusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('LiveOrderStatusScreen')),
      body: const Center(
        child: Text('LiveOrderStatusScreen content goes here'),
      ),
    );
  }
}

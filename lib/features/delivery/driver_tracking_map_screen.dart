import 'package:flutter/material.dart';

class DriverTrackingMapScreen extends StatelessWidget {
  const DriverTrackingMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DriverTrackingMapScreen')),
      body: const Center(
        child: Text('DriverTrackingMapScreen content goes here'),
      ),
    );
  }
}

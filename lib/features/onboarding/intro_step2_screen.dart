import 'package:flutter/material.dart';

class IntroStep2Screen extends StatelessWidget {
  const IntroStep2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('IntroStep2Screen')),
      body: const Center(
        child: Text('IntroStep2Screen content goes here'),
      ),
    );
  }
}

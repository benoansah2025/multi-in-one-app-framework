import 'package:flutter/material.dart';

class PermissionsRequestScreen extends StatelessWidget {
  const PermissionsRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PermissionsRequestScreen')),
      body: const Center(
        child: Text('PermissionsRequestScreen content goes here'),
      ),
    );
  }
}

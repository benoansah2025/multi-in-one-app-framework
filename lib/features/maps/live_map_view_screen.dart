import 'package:flutter/material.dart';

class LiveMapViewScreen extends StatelessWidget {
  const LiveMapViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('LiveMapViewScreen')),
      body: const Center(
        child: Text('LiveMapViewScreen content goes here'),
      ),
    );
  }
}

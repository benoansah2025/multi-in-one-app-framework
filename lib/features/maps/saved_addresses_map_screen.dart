import 'package:flutter/material.dart';

class SavedAddressesMapScreen extends StatelessWidget {
  const SavedAddressesMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SavedAddressesMapScreen')),
      body: const Center(
        child: Text('SavedAddressesMapScreen content goes here'),
      ),
    );
  }
}

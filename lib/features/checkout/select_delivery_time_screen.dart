import 'package:flutter/material.dart';

class SelectDeliveryTimeScreen extends StatelessWidget {
  const SelectDeliveryTimeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SelectDeliveryTimeScreen')),
      body: const Center(
        child: Text('SelectDeliveryTimeScreen content goes here'),
      ),
    );
  }
}
